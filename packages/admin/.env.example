# Admin Portal Environment Variables

# Admin session encryption password (required for nuxt-auth-utils)
# Generate with: openssl rand -base64 32
NUXT_ADMIN_SESSION_PASSWORD=your-secure-session-password-here
